# How subdomains work

a sub domain receives an environment filled with caps representing different interfaces
one of which is an IDomainConfig - by convention this should be the first in the environment

