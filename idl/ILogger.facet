// Domain-bound logging authority. The server supplies attribution.
interface ILogger {
    uuid auto;
    requires IGenericObject;

    // The server supplies domain attribution. Levels use the numeric values
    // from ILoggingConfig.LogLevel.
    method FacetResult log(in i32 level, in string message);
    method FacetResult flush();
}
