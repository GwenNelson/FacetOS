interface ISecurityManager {
    uuid auto;
    requires IGenericObject;

    method FacetResult create_session(in handle authenticated_principal,
                                      out handle session);
}
